import React, { Component } from 'react'
import PropTypes from 'prop-types';

export class PleaseLogin extends Component {
    constructor(){
        super();
    }

    render() {

        return (
            <div>You don't have to pay me, but you do have to log in</div>
        );
    }
}

export default PleaseLogin